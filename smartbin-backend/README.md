# SmartBin Backend

This section contains the Node.js + TypeScript API for SmartBin, including authentication, database access, and real-time socket support.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Notes

- Configure your environment variables using `.env`.
- Use `.env.example` as the template.
