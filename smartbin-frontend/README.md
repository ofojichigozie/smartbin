# SmartBin Frontend

This section contains the React + Vite dashboard for SmartBin, including the user interface for monitoring bin status and readings.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Notes

- Configure the API URL in `.env`.
- Use `.env.example` as the template.
